import { IfAuthenticatedOperatorDirective } from './if-authenticated-operator.directive';

describe('IfAuthenticatedOperatorDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAuthenticatedOperatorDirective();
    expect(directive).toBeTruthy();
  });
});
